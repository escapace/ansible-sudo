---
prerun: false
dependency:
  name: galaxy
driver:
  name: docker
lint: |
    yamllint .
    ansible-lint .
platforms:
  - name: instance
    image: ghcr.io/escapace/ansible-test-containers
provisioner:
  name: ansible
